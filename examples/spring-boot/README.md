# Spring Boot Example

`application.yml`:

```yaml
valuya:
  base: https://pay.gorilla.build
  tenant-token: ttok_...
  default-plan: standard
```

Endpoint `/premium` is protected by the starter filter.
