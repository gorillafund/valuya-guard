# Bot Channel Webhook Runtime Contract

This document has been superseded by:

- [external-soul-runtime-webhook-contract.md](/home/colt/Software/valuya-guard/docs/external-soul-runtime-webhook-contract.md)

Use the newer document as the source of truth. It includes:

- the current `version: "1"` request format
- provider aliases like `n8n`, `langchain`, and `api`
- optional custom webhook headers
- the richer `context` and memory-policy fields
- the runnable local reference server
