# Spring Boot Adapter

Artifact: `build.valuya:valuya-guard-spring-boot-starter`

Provides:
- auto-configured servlet filter
- subject resolver extension point
- optional `@RequiresMandate(resource, plan)` annotation type
