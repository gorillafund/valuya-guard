[project]
name = "valuya-guard"
version = "0.1.0"
description = "Valuya Guard for AWS Lambda (Python)"
requires-python = ">=3.10"
dependencies = ["requests>=2.31.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["valuya_guard*"]
